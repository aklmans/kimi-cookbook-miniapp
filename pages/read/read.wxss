.reader {
  padding: 32rpx 40rpx 48rpx;
}

/* ── top reading progress ── */
.progressbar {
  position: fixed;
  top: 0;
  left: 0;
  height: 4rpx;
  background: var(--accent);
  z-index: 25;
  transition: width 80ms linear;
}

.state {
  min-height: 60vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 32rpx;
  padding: 96rpx 48rpx;
}

.state__text {
  color: var(--ink-3);
  font-size: 28rpx;
  white-space: pre-wrap;
  text-align: center;
  line-height: 1.7;
}

/* skeleton */
.reader__sk-eyebrow {
  width: 160rpx;
  height: 24rpx;
  margin: 24rpx 0 32rpx;
}
.reader__sk-title {
  width: 88%;
  height: 60rpx;
  margin-bottom: 28rpx;
}
.reader__sk-lede {
  width: 100%;
  height: 96rpx;
  margin-bottom: 48rpx;
}
.reader__sk-line {
  width: 100%;
  height: 30rpx;
  margin-bottom: 20rpx;
}
.reader__sk-line--short {
  width: 64%;
  margin-bottom: 40rpx;
}

/* ── end-of-chapter passage ── */
.endcard {
  margin: 0 40rpx;
  padding: 40rpx 0 24rpx;
}

.endcard__kicker {
  text-align: center;
  font-size: 22rpx;
  letter-spacing: 8rpx;
  color: var(--ink-3);
  margin-bottom: 40rpx;
}

.endcard__actions {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  margin-bottom: 32rpx;
}

/* 100% width, single line, arrows outside the text run; margin:0 beats
   the WeChat <button> UA's auto side-margins that shrink the button. */
.endcard__nav {
  width: 100%;
  margin: 0;
  justify-content: flex-start;
  font-size: 15px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.endcard__row {
  display: flex;
  justify-content: center;
  gap: 64rpx;
  padding: 16rpx 0 8rpx;
}

/* book-completion card (replaces the prev/next passage on the last
   chapter once every chapter is finished) */
.endcard__done-title {
  text-align: center;
  font-size: 44rpx;
  line-height: 1.4;
  color: var(--ink);
  margin-bottom: 20rpx;
}

.endcard__done-text {
  text-align: center;
  font-size: 28rpx;
  line-height: 1.75;
  color: var(--ink-2);
  margin-bottom: 40rpx;
}

.endcard__link {
  font-size: 26rpx;
  color: var(--ink-3);
  text-decoration: underline;
  text-decoration-color: var(--accent);
  text-underline-offset: 6rpx;
  background: none;
  border: none;
  padding: 0;
  line-height: 1.6;
}

/* ── off-screen poster canvas ── */
.offscreen-poster {
  position: fixed;
  left: -20000rpx;
  top: 0;
  opacity: 0;
  pointer-events: none;
}

/* ── outline FAB ── */
.fab {
  position: fixed;
  right: 32rpx;
  bottom: 216rpx;
  width: 72rpx;
  height: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--bg-bar);
  backdrop-filter: blur(12px);
  border: 1rpx solid var(--border);
  border-radius: 50%;
  font-size: 34rpx;
  color: var(--ink-2);
  z-index: 20;
  transition: bottom 240ms ease;
}

.fab--down {
  bottom: 48rpx;
}

/* back-to-citation chip (left-side counterpart of the outline FAB) */
.returnchip {
  position: fixed;
  left: 32rpx;
  bottom: 216rpx;
  padding: 16rpx 28rpx;
  background: var(--bg-bar);
  backdrop-filter: blur(12px);
  border: 1rpx solid var(--border);
  border-radius: 999rpx;
  font-size: 24rpx;
  line-height: 1.4;
  color: var(--ink-2);
  z-index: 20;
  transition: bottom 240ms ease;
}

.returnchip--down {
  bottom: 48rpx;
}

/* ── floating bottom bar: icon + label ── */
.barwrap {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 0 24rpx calc(16rpx + env(safe-area-inset-bottom));
  z-index: 10;
}

.bar {
  display: flex;
  border: 1rpx solid var(--border);
  background: var(--bg-bar);
  backdrop-filter: blur(14px);
  padding: 14rpx 0 12rpx;
  transform: translateY(0);
  transition: transform 240ms ease;
}

.bar.is-hidden {
  transform: translateY(calc(100% + 40rpx));
}

.bar__item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
}

.bar__icon {
  font-size: 32rpx;
  line-height: 1;
  color: var(--ink);
}

.bar__icon--font {
  font-size: 26rpx;
  font-weight: 600;
}

.bar__label {
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 18rpx;
  letter-spacing: 2rpx;
  color: var(--ink-3);
}

.bar__item.is-disabled {
  opacity: 0.35;
}

.bottombar__space {
  height: 140rpx;
}

/* ── share sheet ── */
.share-actions {
  padding: 16rpx 32rpx 32rpx;
}

.share-actions__main {
  width: 100%;
  margin-bottom: 28rpx;
}

.share-actions__row {
  display: flex;
  justify-content: space-between;
  padding: 0 8rpx;
  margin-bottom: 20rpx;
}

.share-actions__hint {
  font-size: 22rpx;
  line-height: 1.6;
  color: var(--ink-3);
}

/* ── AI prompt preview (share sheet) ── */
.ai-prompt {
  margin-top: 8rpx;
  padding-top: 20rpx;
}

.ai-prompt__label {
  margin-bottom: 14rpx;
}

.ai-prompt__text {
  max-height: 320rpx;
  overflow-y: auto;
  background: var(--code-bg);
  padding: 20rpx 24rpx;
  font-size: 26rpx;
  line-height: 1.7;
  color: var(--ink-2);
  margin-bottom: 20rpx;
  white-space: pre-wrap;
}

.ai-prompt__row {
  justify-content: flex-start;
  gap: 48rpx;
  padding: 0;
  margin-bottom: 16rpx;
}

/* ── bottom sheet ── */
.sheet-mask {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.32);
  z-index: 30;
  animation: sheet-mask-in 180ms ease-out;
}

@keyframes sheet-mask-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

.sheet {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  max-height: 62vh;
  background: var(--bg);
  border-top: 1rpx solid var(--border);
  z-index: 31;
  transform: translateY(100%);
  transition: transform 240ms ease;
  display: flex;
  flex-direction: column;
  padding-bottom: env(safe-area-inset-bottom);
}

.sheet.is-open {
  transform: translateY(0);
}

/* swipe-down grab strip at the sheet's top edge */
.sheet__grab {
  display: flex;
  justify-content: center;
  padding: 14rpx 0 2rpx;
}

.sheet__grab::before {
  content: "";
  width: 64rpx;
  height: 8rpx;
  border-radius: 999rpx;
  background: var(--border);
}

.sheet__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 32rpx;
}

.sheet__close {
  font-size: 40rpx;
  color: var(--ink-3);
  padding: 0 16rpx;
  line-height: 1;
}

.sheet__scroll {
  max-height: 48vh;
  padding: 0 32rpx 32rpx;
}

.sheet__item {
  padding: 20rpx 0;
  font-size: 28rpx;
  line-height: 1.5;
  color: var(--ink);
  border-bottom: 1rpx solid var(--border);
}

.sheet__item--lv2 {
  font-family: "TsangerJinKai02-W05", "Songti SC", "STSong", serif;
  font-weight: 600;
}

.sheet__item--lv3 {
  padding-left: 40rpx;
  color: var(--ink-2);
  font-size: 26rpx;
}

.sheet__item--hover {
  background: var(--code-bg);
}

/* scroll-spy: the outline entry at the reader's current position */
.sheet__item.is-active {
  color: var(--accent);
  font-weight: 600;
}

.sheet__ref-body {
  padding: 16rpx 32rpx 32rpx;
  font-size: 28rpx;
  line-height: 1.75;
  color: var(--ink-2);
}

.sheet__ref-actions {
  display: flex;
  gap: 48rpx;
  padding: 0 32rpx 32rpx;
}

.sheet__action {
  font-size: 26rpx;
  color: var(--ink);
  text-decoration: underline;
  text-decoration-color: var(--accent);
  text-underline-offset: 6rpx;
}
